<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ $subject ?? 'Mensaje de contacto' }}</title>
</head>
<body>
    <h1>{{ $subject }}</h1>
    <p>{{ $contactMessage }}</p>
</body>
</html>